# Copie este arquivo para .env e ajuste conforme o ambiente.

# URL pública da API FastAPI. Padrão: http://127.0.0.1:8000 (dev local).
# Em produção, aponte para a URL publicada no Render (ou outro host).
VITE_API_URL=http://127.0.0.1:8000
